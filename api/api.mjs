// Declare a constant `apiBase` and assign it the base URL of the API.
const apiBase = 'https://api.noroff.dev/api/v1';

// Declare a constant `auctionEndpoint` and assign it the full URL of the auction endpoint.
// `export` makes `auctionEndpoint` available to be imported by other modules.
export const auctionEndpoint = `${apiBase}/auction`;